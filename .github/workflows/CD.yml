name: CD

on:
  push:
    branches:
      - dev
      - homolog
      - prod

  workflow_dispatch:

# Dois pushes próximos na mesma branch (ex.: dois merges seguidos) disparavam
# dois `cdk deploy` em paralelo contra a MESMA stack do CloudFormation — a
# segunda execução falha com "Cannot delete ChangeSet in status
# CREATE_IN_PROGRESS" (incidente real em 2026-09-10). `cancel-in-progress:
# false` enfileira em vez de cancelar o deploy que já está rodando: cancelar
# um `cdk deploy` no meio corre risco de deixar a stack em estado inconsistente.
concurrency:
  group: cd-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  DeployToAWS:
    environment:
      name: ${{ github.ref_name }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v2
      - name: Setup AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setting stage and stack name
        run: |
          echo "STAGE=${{ github.ref_name }}" 
          echo "STACK_NAME=FormulariosStack${{github.ref_name}}" >> $GITHUB_ENV

      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Generate OpenAPI from Pydantic Contracts
        run: |
          python -m pip install --upgrade pip==26.2.1
          pip install --only-binary :all: pydantic==2.9.2
          PYTHONPATH=. python -m src.shared.helpers.functions.generate_openapi_from_contracts

      - name: Installing Dependencies
        run: |
          npm install -g aws-cdk@2.1139.0 --ignore-scripts
          cd iac
          pip install --only-binary :all: -r requirements.txt

      - name: DeployWithCDK
        run: |
          cd iac
          cdk synth
          cdk bootstrap
          cdk deploy "${{env.STACK_NAME}}" --require-approval never

        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
          STACK_NAME: ${{ env.STACK_NAME }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          USER_POOL_ARN: ${{ secrets.USER_POOL_ARN }}
          USER_POOL_ID: ${{ secrets.USER_POOL_ID }}
          APP_CLIENT_ID: ${{ secrets.APP_CLIENT_ID }}
          BUCKET_NAME: ${{ secrets.BUCKET_NAME }}
          KUMA_HEARTBEAT_PUSH_URL: ${{ secrets.KUMA_HEARTBEAT_PUSH_URL }}
          KUMA_MISSING_FILES_PUSH_URL: ${{ secrets.KUMA_MISSING_FILES_PUSH_URL }}
          RECONCILE_SYSTEMS: ${{ secrets.RECONCILE_SYSTEMS }}
